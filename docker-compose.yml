services:
  mouser-mcp:
    build:
      context: .
      target: production
    image: mouser-mcp:latest
    container_name: mouser-mcp-server
    stdin_open: true
    tty: true      
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
